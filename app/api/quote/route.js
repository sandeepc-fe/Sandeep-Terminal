export async function GET(request) { const symbol = new URL(request.url).searchParams.get('symbol')?.toUpperCase() || 'SPY'; const key = process.env.POLYGON_API_KEY; const base = process.env.POLYGON_BASE_URL || 'https://api.polygon.io'; if (!key) return Response.json({ symbol, price: Number((100 + Math.random() * 900).toFixed(2)), source: 'mock' }); try { const r = await fetch(`${base}/v2/last/trade/${encodeURIComponent(symbol)}?apiKey=${encodeURIComponent(key)}`); const data = await r.json(); const price = data?.results?.p ?? Number((100 + Math.random() * 900).toFixed(2)); return Response.json({ symbol, price, source: 'polygon' }); } catch { return Response.json({ symbol, price: Number((100 + Math.random() * 900).toFixed(2)), source: 'mock' }); } }
