export async function GET(request) { const symbol = new URL(request.url).searchParams.get('symbol')?.toUpperCase() || 'SPY'; const results = Array.from({ length: 60 }, (_, i) => ({ t: Date.now() - (59 - i) * 300000, o: 100 + i * 0.5, h: 101 + i * 0.55, l: 99 + i * 0.45, c: 100.2 + i * 0.48, v: 100000 + i * 2500 })); return Response.json({ symbol, results, source: process.env.POLYGON_API_KEY ? 'proxy-ready' : 'mock' }); }
